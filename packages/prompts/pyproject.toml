[project]
name = "proofhire-prompts"
version = "0.1.0"
description = "Versioned prompt definitions shared by ProofHire's worker tasks"
requires-python = ">=3.13"
dependencies = []

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["proofhire_prompts"]
