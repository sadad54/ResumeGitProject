[project]
name = "proofhire-contracts"
version = "0.1.0"
description = "Canonical enums shared across ProofHire's API and worker"
requires-python = ">=3.13"
dependencies = []

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["proofhire_contracts"]
